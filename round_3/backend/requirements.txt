# PURPOSE: Python dependencies for PARANOID backend
# Keeping it minimal - only what we need

fastapi>=0.116.0  # Requires starlette>=0.45.0 (fixes CVE-2025-27110)
uvicorn>=0.34.0
pydantic>=2.10.0
pillow>=11.0.0
httpx>=0.27.0  # For AI API calls (async HTTP client)
